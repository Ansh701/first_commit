import { SessionExpiredExperience } from "./session-expired-experience";

export const metadata = { title: "Session expired" };

export default function SessionExpiredPage() {
  return <SessionExpiredExperience />;
}

import { VolunteerDirectory } from "@/components/community-flows";

export const metadata = { title: "Volunteer applications" };

export default function Page() {
  return <VolunteerDirectory manage />;
}

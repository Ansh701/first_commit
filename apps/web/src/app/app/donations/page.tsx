import { OrganizationDonationLedger } from "@/components/donation-flows";

export const metadata = { title: "Donations received" };

export default function Page() {
  return <OrganizationDonationLedger />;
}
